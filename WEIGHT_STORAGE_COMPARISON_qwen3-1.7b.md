# Qwen3-1.7B 权重存储大小对比（W16A16 / W4A16 / W3A16）

> 分析日期：2026-09-15
> 模型：`Qwen/Qwen3-1.7B`（Dense）

## 一、模型参数量分解

Qwen3-1.7B 关键结构参数：

| 参数 | 值 |
| --- | --- |
| hidden_size | 2048 |
| intermediate_size | 6144 |
| num_hidden_layers | 28 |
| num_attention_heads | 16 |
| num_key_value_heads | 8 |
| head_dim | 128 |
| vocab_size | 151936 |
| tie_word_embeddings | true |

每层线性层权重（`[out, in]`）：

| 层 | 形状 | 参数量 |
| --- | --- | --- |
| q_proj | 2048 × 2048 | 4,194,304 |
| k_proj | 1024 × 2048 | 2,097,152 |
| v_proj | 1024 × 2048 | 2,097,152 |
| o_proj | 2048 × 2048 | 4,194,304 |
| gate_proj | 6144 × 2048 | 12,582,912 |
| up_proj | 6144 × 2048 | 12,582,912 |
| down_proj | 2048 × 6144 | 12,582,912 |
| **每层合计** | — | **50,331,648** |

- 28 层线性层：`50,331,648 × 28 = 1,409,286,144`
- 词嵌入（与 lm_head 共享，`tie_word_embeddings=true`）：`151936 × 2048 = 311,164,928`
- **总参数量 ≈ `1,720,451,072`（≈1.72B）**

## 二、权重存储大小（核心对比）

| 方案 | 位宽 | 每参数字节 | 权重大小 | 相对 W16A16 |
| --- | --- | --- | --- | --- |
| W16A16 | 16 bit | 2.0 B | 3.44 GB（3.20 GiB） | 1.00×（基准） |
| W4A16 | 4 bit | 0.5 B | 0.86 GB（0.80 GiB） | **≈ 4.0×** |
| W3A16 | 3 bit | 0.375 B | 0.65 GB（0.60 GiB） | **≈ 5.33×** |

> 注：3-bit 非字节对齐（每值 0.375 字节），实际工程打包通常按 4-bit 对齐或按组打包，会有轻微对齐损耗；上表为理论值。

## 三、量化附加开销

weight-only（lora_only）模式下，除量化权重外还有元数据开销（per-channel）：

| 组件 | 数量 | 存储 | 大小 |
| --- | --- | --- | --- |
| scale（每输出通道 1 个，fp16） | 573,440 | 2 B | 1.15 MB |
| zero-point（非对称，每通道 1 个） | 573,440 | 2 B | 1.15 MB |
| LWC 边界因子（up/lowbound，各 1） | 1,146,880 | 2 B | 2.29 MB |
| LoRA（rank 4，A+B） | 4,358,144 | 2 B | 8.72 MB |

> 输出通道总数 = `(2048+1024+1024+2048+6144+6144+2048) × 28 = 573,440`

**附加开销合计 ≈ 13.3 MB**（其中 LoRA 占 8.72 MB，其余量化元数据 ~4.6 MB）。

## 四、实际总存储（含开销）

| 方案 | 量化权重 | 量化元数据(scale/zp/lwc) | LoRA | 总计 | 压缩率 |
| --- | --- | --- | --- | --- | --- |
| W16A16 | 3.44 GB | — | — | **3.44 GB** | 1.00× |
| W4A16 | 0.86 GB | 4.59 MB | 8.72 MB | **≈ 0.87 GB** | **≈ 3.94×** |
| W3A16 | 0.65 GB | 4.59 MB | 8.72 MB | **≈ 0.66 GB** | **≈ 5.21×** |

结论：量化元数据 + LoRA 的总开销（~13 MB）相对权重本身（0.65–0.86 GB）不足 2%，压缩率基本由位宽决定。

## 五、实验佐证（slider_parameters.pth 实际大小）

本仓库 `lora_only` 模式下，checkpoint 只保存「量化配方」（LoRA + scale + LWC），**不保存**量化后的 int 权重（int 权重由 fp16 权重 + 量化器在推理时重建）：

| 实验 | slider_parameters.pth | 说明 |
| --- | --- | --- |
| W3A16 | 22,274,491 B（21.2 MiB） | 与 W4A16 **完全一致** |
| W4A16 | 22,274,491 B（21.2 MiB） | 与 W3A16 **完全一致** |
| W3A8（slider 模式） | 24,987,327 B（23.8 MiB） | 额外含 LET smooth 参数 |

关键观察：**W3A16 与 W4A16 的 checkpoint 大小相同**，因为 LoRA + 每通道 scale/LWC 的元数据数量与位宽无关；二者真正的存储差异体现在部署时的 int 权重（3-bit vs 4-bit），而非 checkpoint。

## 六、总结

1. **纯权重存储**：W4A16 约为 FP16 的 1/4，W3A16 约为 1/5.33。
2. **实际总存储**（含 scale/zero-point/LWC/LoRA）：W4A16 ≈ 0.87 GB（3.94×），W3A16 ≈ 0.66 GB（5.21×）。
3. **W4A16 vs W3A16**：权重部分 W3A16 比 W4A16 再省 ~25%（0.65 vs 0.86 GB），但精度损失也更大（实测 PPL：18.07 vs 22.18）。
4. 附加开销（~13 MB）可忽略，压缩收益主要来自权重位宽降低。
