# Qwen3-8B W3A16 中间层量化 — 量化 vs 全精度对比报告

> 实验：只量化中间层 layer 6~29（24 层）的 W3A16，对比 FP16 全精度基线。
> 日期：2026-09-18

## 一、核心结果

| 配置 | 量化层数 | wikitext2 PPL | avg-6 ↑ | 训练耗时 |
| --- | --- | --- | --- | --- |
| **FP16 基线** | 0 | **9.730** | **74.01%** | — |
| **W3A16-mid（训练后）** | 24/36 | **10.655** | **71.72%** | 8.09h（36 轮） |
| W3A16-mid RTN（无训练） | 24/36 | 33.08 | — | — |

**主要结论：**

1. **精度损失极小**：量化后 PPL 仅从 9.730 升至 10.655（**退化 9.5%**），avg-6 仅降 2.29pp（74.01%→71.72%）。
2. **训练有效**：RTN（无训练）PPL 33.08 → 训练后 10.655，收益 3.1×（首窗对齐补丁在 8B 上同样生效）。
3. **存储压缩**：部署后约 8.86 GB vs FP16 16.4 GB，**减少 46%（1.85×）**。

## 二、六任务明细

| 任务 | FP16 | W3A16-mid | 差值 |
| --- | --- | --- | --- |
| piqa | 0.7769 | 0.7606 | -0.016 |
| arc_easy | 0.8081 | 0.7875 | -0.021 |
| arc_challenge | 0.5657 | 0.5520 | -0.014 |
| boolq | 0.8654 | 0.8422 | -0.023 |
| hellaswag | 0.7487 | 0.7018 | -0.047 |
| winogrande | 0.6756 | 0.6590 | -0.017 |
| **avg-6** | **74.01%** | **71.72%** | **-2.29pp** |

## 三、训练有效性（补丁前后）

| 版本 | 输入 | PPL | avg-6 |
| --- | --- | --- | --- |
| RTN（无训练） | — | 33.08 | — |
| **v1（首窗对齐补丁）** | **对齐（layer 0–5 输出）** | **10.655** | **71.72%** |

- 首窗对齐补丁（`partial-quant-fix`）在 Qwen3-8B 上同样生效：首窗 [6] 训练输入正确对齐到 fp16 layer 0–5 输出，首窗 loss 0.047（与 1.7B 的 0.034 同量级）。
- 训练 8.09h，36 轮（18 窗 × 2 stage），0 次 OOM / 0 次重试，显存峰值仅 25.5GB（64GB HBM 内）。

## 四、存储对比

| 方案 | 存储 | 压缩 |
| --- | --- | --- |
| FP16（8.2B 参数） | 16.4 GB | 1.00× |
| **W3A16-mid 部署** | **≈ 8.86 GB** | **1.85×（减少 46%）** |

分解：量化 24 层 int3（4.63B 参数 → 1.74GB）+ 未量化 12 层 fp16（4.63GB）+ emb/lm_head fp16（2.49GB）+ 元数据（~2MB）。

## 五、与 Qwen3-1.7B 的可扩展性对照

| 模型 | 量化层 | PPL（FP16→量化） | avg-6 损失 |
| --- | --- | --- | --- |
| Qwen3-1.7B | 16/28 | 16.71 → 17.80（+6.5%） | -2.44pp |
| **Qwen3-8B** | 24/36 | **9.73 → 10.66（+9.5%）** | **-2.29pp** |

两个规模下「中间层 W3A16 量化」的精度损失均控制在 ~10% PPL 以内、avg-6 损失 ~2.3pp，方法在更大模型上依然稳定有效。

## 六、结论

1. **Qwen3-8B 中间层 W3A16 量化**：PPL 10.655 / avg-6 71.72%，逼近 FP16，损失在可接受范围。
2. **首窗对齐补丁跨规模有效**：从 1.7B 到 8B，无需改动，训练均为正收益。
3. **压缩与精度可平衡**：量化 24/36 层获得 46% 存储压缩，代价仅 ~9.5% PPL 退化；可通过调整量化层范围进一步权衡。

## 七、产物位置

| 产物 | 位置 |
| --- | --- |
| 量化配置 | `configs/qwen3-8b-w3a16-mid/config.yaml` |
| checkpoint | 服务器 `log/qwen3-8b-w3a16-mid/slider_parameters.pth` |
| 训练日志 | 服务器 `/workspace/quant-research/qwen3_8b_w3a16_mid.log` |
| 评测日志 | 服务器 `/workspace/quant-research/qwen3_8b_w3a16_mid_eval.log` |