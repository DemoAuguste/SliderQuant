# Qwen3-1.7B W3A16 中间层量化 — 三方对比报告

> 实验：只量化中间层 layer 6~21（16 层）的 W3A16，对比 FP16 基线与全量化 W3A16-full（28 层）。
> 更新时间：2026-09-18

## 一、核心结果

| 配置 | 量化层数 | wikitext2 PPL | avg-6 ↑ | 训练耗时 |
| --- | --- | --- | --- | --- |
| **FP16 基线** | 0 | **16.71** | **64.08%** | — |
| **W3A16-mid v3**（中间 16 层） | 16 | **17.80** | **61.64%** | 3.01h（28 轮） |
| **W3A16-full**（全 28 层） | 28 | 22.18 | 57.70% | — |

**主要发现：**

1. **中间层量化逼近 FP16**：mid v3 的 PPL 17.80 距 FP16 16.71 仅 +6.5%，avg-6 仅降 2.44pp。
2. **mid 优于 full**：量化 16 层（17.80）比量化 28 层（22.18）精度更高——量化层越少、损失越小，符合直觉；代价是压缩率更低（16/28 层量化）。
3. **训练式 PTQ 必需**：mid 无训练（RTN）PPL 高达 64.96，训练后降至 17.80，收益 3.6×。

## 二、六任务明细（6 项零样本）

| 任务 | FP16 | W3A16-mid v3 | W3A16-full |
| --- | --- | --- | --- |
| piqa | — | 0.7067 | — |
| arc_easy | — | 0.6852 | — |
| arc_challenge | — | 0.3993 | — |
| boolq | — | 0.7657 | — |
| hellaswag | — | 0.5487 | — |
| winogrande | — | 0.5927 | — |
| **avg-6** | **64.08%** | **61.64%** | **57.70%** |

> 注：FP16 / FULL 的单任务明细见各自报告（`FP16_BASELINE_qwen3-1.7b.md`、`W3A16_vs_FP16_qwen3-1.7b.md`）。

## 三、训练有效性：补丁前后

| 版本 | 窗口数/轮次 | 输入 | PPL | avg-6 |
| --- | --- | --- | --- | --- |
| RTN（无训练） | — | — | 64.96 | — |
| v1（7 窗） | 14 轮 | 错位（embedding 输出） | 72.75 | 47.74% |
| v2（14 窗） | 28 轮 | 错位（embedding 输出） | 153.65 | 46.71% |
| **v3（14 窗 + 补丁）** | 28 轮 | **对齐（layer 0–5 输出）** | **17.80** | **61.64%** |

- **训练越多越差（v1→v2）**：错位输入上训练，参数越「自信」破坏越大。
- **一根因**：部分层量化时，SliderQuant 训练循环把 `inps`（layer 0 输入）直接喂给首窗层（layer 6），未先前向到首窗起始层，导致训练输入分布与推理错位。
- **一处补丁**：首窗起始层 > 0 时，先把 `inps` 通过前置 fp16 层 [0, start) 前向（见 [sliderquant.py](file:///c:/code/SliderQuant/quantize/sliderquant.py#L496) 附近 `partial-quant-fix` 注释块）。

## 四、结论

1. **中间层量化（W3A16-mid）在修复首窗输入错位后，PPL 17.80 / avg-6 61.64%，逼近 FP16 且优于全量化 FULL** —— 是「精度/压缩」曲线上的有效中间点。
2. **SliderQuant 框架存在「部分层量化首窗输入错位」的代码级 bug**，只影响首窗起始层 > 0 的场景（FULL 起始层=0 不受影响）。已定位并修复，补丁对 FULL 无副作用。
3. **可复用经验**：做部分层量化时，务必确认首窗输入是「前置层真实输出」而非 embedding 输出；用「深层窗口 loss 是否与 FULL 对应层同量级」作为快速自检。

## 五、产物位置

| 产物 | 位置 |
| --- | --- |
| 补丁后训练代码 | `quantize/sliderquant.py`（`partial-quant-fix` 注释块） |
| v3 checkpoint | 服务器 `log/qwen3-1.7b-w3a16-mid-v3/slider_parameters.pth` |
| 训练/评测日志 | 服务器 `/workspace/quant-research/w3a16_mid_v3*.log` |
| 进度记录 | `PROGRESS_qwen3-1.7b-w3a16-mid.md` |